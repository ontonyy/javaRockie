spring:
  application:
    name: client-en
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
